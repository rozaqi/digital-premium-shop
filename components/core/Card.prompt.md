Base white surface card — the building block for product/category/testimonial panels.

```jsx
<Card interactive>…</Card>
<Card featured padding="var(--space-8)">Paket terlaris</Card>
```

`interactive` adds hover lift + shadow grow. `featured` adds an indigo ring. Override `padding`/`radius` with CSS values; `as` changes the element.
